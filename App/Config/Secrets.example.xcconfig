// Copy this file to Secrets.xcconfig (same directory) and fill in values.
// Secrets.xcconfig is gitignored — NEVER commit real keys.
// No MVP feature requires a key; these enable optional adapters only.

// Roboflow cloud detection adapter (optional; default detection is on-device).
ROBOFLOW_API_KEY =

// Claude coaching adapter (post-MVP, optional).
ANTHROPIC_API_KEY =
