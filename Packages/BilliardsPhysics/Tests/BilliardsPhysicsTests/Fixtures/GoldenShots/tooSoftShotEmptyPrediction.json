{
  "name": "tooSoftShotEmptyPrediction",
  "description": "initialSpeed equal to restSpeed (0.05) fails the launch guard: the prediction is completely empty.",
  "derivation": [
    "The solver only simulates when initialSpeed > config.restSpeed; 0.05 > 0.05 is false.",
    "Expected: no segments, no events, no pocketed balls."
  ],
  "table": {
    "size": "nineFoot"
  },
  "config": {
    "rollingDeceleration": 0.5,
    "cushionRestitution": 0.75,
    "ballBallRestitution": 0.96,
    "restSpeed": 0.05
  },
  "balls": [
    {
      "id": 0,
      "kind": "cue",
      "position": [
        0.0,
        0.0
      ]
    }
  ],
  "aim": {
    "origin": [
      0.0,
      0.0
    ],
    "direction": [
      1,
      0
    ]
  },
  "options": {
    "initialSpeed": 0.05,
    "maxEvents": 8
  },
  "tolerance": 1e-09,
  "expected": {
    "segments": [],
    "events": [],
    "pocketedBalls": []
  }
}
