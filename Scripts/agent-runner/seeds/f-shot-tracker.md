## Context
The on-device detector is blind to balls at shot speed, so real trajectories come from offline 240 fps video processed without a time budget.

## Scope
`Tools/ShotTracker/track_shot.py` (OpenCV, containerised), checkerboard undistortion, per-frame homography re-fit from ≥ 6 diamonds on the ball-centre plane with reported residual, output `Fixtures/Reference/shots/<name>.json` in the `ReferenceShot` schema; `Packages/BilliardsPhysics` `ReferenceComparisonTests` (departure angle, first-cushion exit; rest/pockets reported not gated).

## Out of scope
pooltool (deferred until after the Solver v2 contract PR); any solver constant change without a moved eval row.

## Acceptance
- [ ] Synthetic test video (rendered moving disc over a table image) is tracked to ≤ 5 mm RMS.
- [ ] `docs/validation/physics-eval.md` is generated by the tool with the initial gates.

## Verification
verify:unit for the Swift side; the Python tool has its own pytest run in the container.

## Device follow-up
Owner: night-2 slow-mo session (six shots, iPhone near-nadir, stabilization off).
